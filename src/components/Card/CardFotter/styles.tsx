import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#2d1d31",
        borderRadius: 8,
        borderColor: "#b788fc",
        borderWidth: 1,
        width: "100%",
        marginBottom: 10,
    },
});